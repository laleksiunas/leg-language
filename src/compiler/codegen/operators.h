#pragma once

namespace codegen::operators {
    const auto Plus = "+";
    const auto Minus = "-";
    const auto Multiply = "*";
    const auto Divide = "/";
    const auto Modulus = "%";
    const auto And = "&&";
    const auto Or = "||";
    const auto Equal = "==";
    const auto NotEqual = "!=";
    const auto Greater = ">";
    const auto GreaterEqual = ">=";
    const auto LessThan = "<";
    const auto LessThanEqual = "<=";
}
